spring:
  application:
    name: greeting-service

management:
  endpoint:
  endpoints:
    web:
      exposure:
        include:
          - '*'

greeting-service:
  greeting: "Hola"
  default-value: "Mundo"